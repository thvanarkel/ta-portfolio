Wordpress theme for my personal portfolio

For use clone the repository then `$ npm install`
For development mode:
`$ npm run start`
For building production files:
`$ npm run build` and grab the zip file from /bundled
